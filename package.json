{
  "name": "interview-coach-skill",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "AI job search copilot: resume builder, job matching, interview coach, and application tracker.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "smoke:live": "node tests/live-smoke.mjs",
    "serve": "python -m http.server 8788 -d web-app"
  }
}
